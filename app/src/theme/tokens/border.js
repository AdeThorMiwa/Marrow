// Plain JS — see colors.js for why.
const borderWidth = 1; // Req 4.1 — the only separation mechanism; never a shadow
const borderWidthError = 2; // TextInput error/focus state (Design §4.4) — weight, not color

module.exports = { borderWidth, borderWidthError };
